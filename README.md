# Docker container version of sonos-web for Raspberry Pi
**WORK-IN-PROGRESS** 

## Usage
```
docker run -d \
  --net=host \
  --restart=always \
  --name=sonos-web \
  psychlist/docker-sonos-web-arm
  ```
